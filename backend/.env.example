DATABASE_URL="postgresql://postgres:postgres@localhost:5432/buddychecks_dev"

# Auth0 — fill in from your Auth0 dashboard
AUTH0_DOMAIN="your-tenant.auth0.com"
AUTH0_AUDIENCE="your-api-identifier"

# CORS — set to your frontend's origin (no trailing slash)
# Development default is http://localhost:3000 when this is unset
FRONTEND_URL="http://localhost:3000"

PORT=4000
